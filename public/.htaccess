Options -MultiViews
RewriteEngine On
RewriteBase /

RewriteCond %{HTTPS} !=on
RewriteCond %{HTTP_HOST} ^vendor\.pataspare\.co\.ke$ [OR]
RewriteRule .* https://vendor.pataspare.co.ke%{REQUEST_URI} [R=301,L]

RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.html [QSA,L]
